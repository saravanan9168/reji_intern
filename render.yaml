services:
  - type: web
    name: math-assistant
    env: docker
    plan: free
    branch: main
    dockerfilePath: Dockerfile
    autoDeploy: true
    # Optional: health check path Render will poll to determine service health
    healthCheckPath: /
