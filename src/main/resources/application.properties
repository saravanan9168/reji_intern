## When deployed to Render, Render provides the port in the $PORT env var.
## Use that value when present, otherwise default to 8090 for local runs.
server.port=${PORT:8090}
spring.thymeleaf.cache=false
spring.thymeleaf.check-template-location=false